namespace exemplo2 {
    //var, let const
    let numero: number;
    numero = 5;

    let resultado: number;
    resultado = numero + numero

    console.log('A soma entre ' + numero + ' e ' + numero + ' é igual a ' + resultado);

    resultado = numero - 4;
    console.log(`A subtração entre ${numero} e 4 é igual a ${resultado}`);

    resultado = numero *3;
    console.log(`${numero} x 3 = ${resultado}`)

    resultado = numero / 2;
    console.log(`${numero} dividido por 2 é igual a ${resultado}`);
}